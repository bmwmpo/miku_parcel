{
  "devDependencies": {
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "textalive-app-api": "^0.3.2"
  },
  "scripts":{
    "build-dev" : "parcel ./index.html --dist-dir dev",
    "clean": "del-cli .parcel-cache dev docs",
    "prebuild": "del-cli docs"
  }
}
